//Attempted to change all instances of 's' and 'S' with '$'

// HTMLElement.prototype.textNodes = function() {
//     return [...this.childNodes].filter((node) => {
//       return (node.nodeType === Node.TEXT_NODE && node.nodeValue.trim() !== "");
//     });
//   }

//   document.querySelector('#testSubject').textNodes().forEach((node) => {
//     node.textContent = 'S';});

// const mono = "To be, or not to be--that is the question: Whether tis nobler in the mind to suffer. The slings and arrows of outrageous fortune Or to take arms against a sea of troubles";

// const newMono = mono.split('');



// const finalMono = newMono.forEach((element) => { if(element === "s" || element === "S") element = '$';}).toString().replace(",", "");

// console.log(finalMono);


 



